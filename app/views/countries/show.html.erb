<h1> <%= @country.name%> </h1>

<div class= "main_image">
  <img src = <%= @country.image%> style="width:500px">
</div>

<div class= "instruct">
  <h4> Below, you can find listed the cities you may visit in <%=@country.name%> </h4>
</div>
<% @country.cities.each do |city| %>
  <h3><%= link_to city.name, country_city_path(country_id: @country.id, id: city.id)%>
<% end %>

